# Select backend (API-only for now)

Select backend (API-only for now)

## Usage

``` r
set_backend(backend = c("api"))
```

## Arguments

- backend:

  Character scalar; only "api" is currently supported.
