% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{rxref_cache}
\alias{rxref_cache}
\alias{rxref_cache_info}
\alias{rxref_cache_clear}
\alias{rxref_cache_use_memory}
\alias{rxref_cache_use_disk}
\alias{rxref_cache_disable}
\title{Manage the rxref cache}
\usage{
rxref_cache()

rxref_cache_info()

rxref_cache_clear()

rxref_cache_use_memory(
  max_size = 200 * 1024^2,
  max_age = Inf,
  evict = c("lru", "fifo")
)

rxref_cache_use_disk(
  dir = rxref_cache_dir(),
  max_size = 1024^3,
  max_age = Inf,
  evict = c("lru", "fifo")
)

rxref_cache_disable()
}
\arguments{
\item{max_size}{Maximum cache size in bytes.}

\item{max_age}{Maximum age of cached objects, in seconds.}

\item{evict}{Cache eviction policy passed to \code{cachem::cache_mem()}.}

\item{dir}{Directory to use for an on-disk cache.}
}
\value{
\code{rxref_cache()} returns a \code{cachem} cache object.

\code{rxref_cache_info()} returns a tibble with cache metadata.

\code{rxref_cache_clear()}, \code{rxref_cache_use_memory()}, \code{rxref_cache_use_disk()},
and \code{rxref_cache_disable()} invisibly return the configured cache object.
}
\description{
\code{rxref_cache()} returns the currently configured cache object.
\code{rxref_cache_info()} returns a small summary of the current cache.
\code{rxref_cache_clear()} clears cached RxNorm/RxClass API responses.
\code{rxref_cache_use_memory()} switches rxref to an in-memory cache.
\code{rxref_cache_use_disk()} switches rxref to an on-disk cache.
\code{rxref_cache_disable()} uses an immediately expiring cache, effectively
disabling cache reuse for the current R session.
}
\details{
rxref caches memoised API responses through a \code{cachem} cache object stored in
\code{getOption("rxref.cache")}. By default, rxref uses an in-memory cache for the
current R session.
}
\examples{
rxref_cache_info()

rxref_cache_clear()

rxref_cache_use_memory()

\dontrun{
rxref_cache_use_disk()
}

}
